export default function Home() {
  return (
    <main className="container-x py-32">
      <p className="text-eyebrow font-sans uppercase text-ember">Typography check</p>
      <h1 className="font-display text-h1 text-mist mt-6">Build it <em className="italic font-light text-ember">so it runs.</em></h1>
      <p className="mt-8 max-w-2xl text-body text-fog">Fraunces display + Plus Jakarta Sans body. Ember selection + accent. Dark ink canvas.</p>
    </main>
  )
}
